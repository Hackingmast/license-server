import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.studybuddy_pro',
  appName: 'studybuddy-pro',
  webDir: 'out', // ✅ Next.js export output folder
};

export default config;
